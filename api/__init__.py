# Marks this directory as a Python package so `from api.app import app`
# resolves reliably everywhere (pytest, uvicorn, etc.), regardless of
# how the interpreter was invoked or what the current working
# directory is.
